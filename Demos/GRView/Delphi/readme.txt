GRView is a utility for view .grt and .grw files.
If extentions .grw and .grt not registered, GRView
ask fo registrer this extentions. To register extentions
you may also run GRView with reg parameter, example:
>grview.exe reg